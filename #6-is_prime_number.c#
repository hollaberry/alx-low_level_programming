#include "main.h"
/**
 * is_prime_number - Function to recursively get a prime number
 * @n: Number to check if its a number
 *Return: Always 0
 */
int is_prime_number(int n)
{
return (check_prime(n,2));
}

int check_pr