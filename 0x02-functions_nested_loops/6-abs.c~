#include "main.h"
/**
 * _abs -function to check the absolute value of an integer
 * @c: This is the integer to be check for its absolute value
 * @r: Takes the abs function
 * Return: Always the abs value of the number
 */
int _abs(int c)
{
int r;
r = abs(c);
return (r);
}
