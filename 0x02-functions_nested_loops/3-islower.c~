#include "main.h"
/**
 * _islower -function to check if a character is a lower case alphabet
 *
 * Return Always 1 if character is lower case and 0 is otherwise
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
