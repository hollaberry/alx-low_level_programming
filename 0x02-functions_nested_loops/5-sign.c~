#include "main.h"
/**
 * _isalpha -function to check if a character is a lower case alphabet
 * @c: This is the character to be checked
 * Return: Always 1 if character is an alphabet and 0 if otherwise
 */
int _isalpha(int c)
{
return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
}
