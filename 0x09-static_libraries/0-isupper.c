#include "main.h"
#include <stdio.h>
/**
 * _isupper -function to check for upper case character
 * @c: This is the integer to be check for it upper case character
 * Return: Always 1 (success)
 */
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
