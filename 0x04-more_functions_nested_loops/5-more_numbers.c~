#include "main.h"
/**
 * print_numbers -function to print numbers from 0 to 9
 * Return: Always 1 (success)
 */
void print_numbers(void)
{
  int i, n, m, j;
for (i = 0; i <= 10; i++)
{
  n = i/10;
  m = i%10;
  for (
_putchar(i);
}
_putchar('\n');
}
