#include "main.h"
#include <stdio.h>
/**
 * _isdigit -function to check if a character is a digit
 * @c: This is the integer to be check if its a digit
 * Return: Always 1 (success)
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
